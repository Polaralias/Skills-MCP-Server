runtime: "container"
build:
  dockerfile: "Dockerfile"
  dockerBuildPath: "."
startCommand:
  type: "http"
  port: 3000
  healthCheckPath: "/health"
  path: "/mcp"
  exampleConfig:
    skillsDirectory: "./skills"
